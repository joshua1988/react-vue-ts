// @ts-check

// document.getElementById('')

document.querySelector('fieldset');

/**
 * @description 두개의 숫자를 더하는 함수
 * @param a 
 * @param b 
 * @returns 
 */
function sum(a: number, b: number) {
  return a + b;
}

// sum(10, '20');